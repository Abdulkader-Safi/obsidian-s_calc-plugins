import Root from './button.svelte';
import {
	buttonVariants,
	type ButtonVariant,
	type ButtonSize,
} from './variants';

export {
	Root,
	Root as Button,
	buttonVariants,
	type ButtonVariant,
	type ButtonSize,
};
